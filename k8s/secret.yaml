apiVersion: v1
kind: Secret
metadata:
  name: hairgen-secrets
  namespace: hairgen
type: Opaque
stringData:
  # --- Clerk ---
  NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: "pk_test_REPLACE_ME"
  CLERK_SECRET_KEY: "sk_test_REPLACE_ME"

  # --- Database ---
  DATABASE_URL: "postgresql://user:password@hairgen-db:5432/hairgen"

  # --- Cloudflare R2 ---
  R2_ACCOUNT_ID: "REPLACE_ME"
  R2_ACCESS_KEY_ID: "REPLACE_ME"
  R2_SECRET_ACCESS_KEY: "REPLACE_ME"
  R2_BUCKET_NAME: "hairgen"
  R2_PUBLIC_URL: "https://your-bucket.r2.dev"

  # --- fal.ai ---
  FAL_KEY: "REPLACE_ME"

  # --- Stripe ---
  STRIPE_SECRET_KEY: "sk_test_REPLACE_ME"
  STRIPE_WEBHOOK_SECRET: "whsec_REPLACE_ME"
  NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: "pk_test_REPLACE_ME"

  # --- Inngest ---
  INNGEST_EVENT_KEY: "REPLACE_ME"
  INNGEST_SIGNING_KEY: "REPLACE_ME"

  # --- App ---
  NEXT_PUBLIC_APP_URL: "http://152.53.116.238:30320"
